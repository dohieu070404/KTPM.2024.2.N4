server:
  port: 8080
  servlet:
    context-path: /bookingtravel
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/bookingtravel"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
